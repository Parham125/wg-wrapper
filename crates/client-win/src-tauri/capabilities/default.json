{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs only, no plugins.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
